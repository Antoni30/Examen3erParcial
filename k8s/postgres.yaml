apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: microservicios-agricultura
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: tonir30/bdmicro:latest
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: admin
            - name: POSTGRES_PASSWORD
              value: adminadmin123
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          emptyDir: {}  # Usa PersistentVolume si necesitas persistencia real

---

apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: microservicios-agricultura
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP